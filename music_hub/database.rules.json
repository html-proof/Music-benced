{
    "rules": {
        "users": {
            "$uid": {
                // Allow only the authenticated user to read/write their own data
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid"
            }
        },
        // Deny access to everything else by default
        ".read": false,
        ".write": false
    }
}